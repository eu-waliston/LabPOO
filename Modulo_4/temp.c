#include <stdio.h>
int main(){
    float PI = 3.14;
    float raio = 10;

    float circ = 2 * PI * raio;
    float area = PI * raio * raio;

    printf("Á area do circulo é \%f e a sua circunferência pe \%f.", area, circ);
}